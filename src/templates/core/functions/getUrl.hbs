export function getUrl(base: string, bizPath: string, query?: Record<string, any>): string {
    const path = bizPath.replace(/[:]/g, '_');
    const url = `${base}${path}`;

    if (query) {
        return `${url}${getQueryString(query)}`;
    }
    return url;
}
